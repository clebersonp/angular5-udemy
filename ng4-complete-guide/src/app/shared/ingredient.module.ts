export class Ingredient {
     // por debaixo dos panos sera gerado as propriedades name e amount
    // public name: string;
    // public amount: number;

    constructor(public name: string, public amount: number) {
        // por debaixo dos panos sera gerado as propriedades name e amount
        // this.name = name;
        // this.amount = amount;
    }
}
